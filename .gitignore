# Xcode
xcuserdata/
DerivedData/
build/output/
!build/config/

# macOS
.DS_Store
*.swp
*~

# Material (source data, large)
material/

# Audio & binary assets are tracked by Git LFS (see .gitattributes)

# Store scripts (contains no secrets but not needed in app repo)
store/fix_iap.py

# Generated Xcode project (use xcodegen to regenerate)
Dictation.xcodeproj/

# Utility scripts (not part of the app)
fix_blanks_*.py
fix_blanks2.py
generate_tts.py
setup_lfs.sh
